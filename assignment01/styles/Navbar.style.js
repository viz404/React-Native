import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
  nav: {
    paddingTop: 10,
    flexDirection: "column",
  },
  buttonText: {
    fontSize: 35,
    fontWeight: "bold",
    color: "silver",
  }
})